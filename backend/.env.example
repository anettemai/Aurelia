# Example environment variables for backend
# Copy this file to .env and fill in the real values (do NOT commit your .env)

DB_USER=anettesamolberg
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aurelia_db

# Optional: PORT the express server listens on
PORT=5001
